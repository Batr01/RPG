# Настройка Git LFS для библиотек

Этот файл содержит инструкции по настройке Git LFS для библиотек Kevin Iglesias и TriForge Assets.

## 📋 Что уже настроено

✅ Создан файл `.gitattributes` с правилами для Git LFS
✅ Настроены правила для следующих типов файлов:
   - 3D модели: `.fbx`
   - Текстуры: `.png`, `.jpg`, `.jpeg`, `.tga`, `.psd`, `.exr`, `.hdr`
   - Unity пакеты: `.unitypackage`
   - Архивы: `.zip`
   - Документы: `.pdf`
   - Аудио и видео файлы (если есть)

✅ Все файлы в папках `Assets/Kevin Iglesias/` и `Assets/TriForge Assets/` настроены на использование Git LFS

## 🔧 Установка Git LFS

### macOS (через Homebrew)
```bash
brew install git-lfs
```

### macOS (через MacPorts)
```bash
sudo port install git-lfs
```

### Альтернативный способ (универсальный)
1. Скачайте установщик с официального сайта: https://git-lfs.github.com/
2. Следуйте инструкциям установщика

## 🚀 Инициализация Git LFS в проекте

После установки Git LFS выполните следующие команды:

```bash
# 1. Инициализация Git LFS в репозитории
git lfs install

# 2. Проверка, что Git LFS работает
git lfs version

# 3. Проверка, какие файлы будут отслеживаться через LFS
git lfs track
```

## 📦 Миграция существующих файлов

Если у вас уже есть коммиты с большими файлами, их нужно мигрировать:

```bash
# 1. Убедитесь, что все изменения закоммичены или спрятаны
git status

# 2. Миграция существующих файлов в Git LFS
# Это перепишет историю, поэтому будьте осторожны!
git lfs migrate import --include="*.fbx,*.png,*.jpg,*.jpeg,*.tga,*.psd,*.exr,*.hdr,*.unitypackage,*.zip,*.pdf" --everything

# ИЛИ только для папок библиотек:
git lfs migrate import --include="Assets/Kevin Iglesias/**" --everything
git lfs migrate import --include="Assets/TriForge Assets/**" --everything
```

⚠️ **Внимание**: Команда `git lfs migrate` переписывает историю Git. Если вы уже отправили коммиты в удаленный репозиторий, вам нужно будет сделать force push:
```bash
git push --force --all
git push --force --tags
```

## ✅ Проверка работы Git LFS

```bash
# Проверить, какие файлы отслеживаются через LFS
git lfs ls-files

# Проверить статус LFS
git lfs status
```

## 📝 Добавление файлов в Git

После настройки Git LFS, добавление файлов происходит как обычно:

```bash
# Добавить все файлы
git add .

# Или добавить конкретные файлы
git add Assets/Kevin\ Iglesias/
git add Assets/TriForge\ Assets/

# Проверить, что файлы правильно отслеживаются
git lfs ls-files

# Закоммитить
git commit -m "Add libraries with Git LFS"
```

## 🔍 Полезные команды Git LFS

```bash
# Показать все файлы, отслеживаемые через LFS
git lfs ls-files

# Показать информацию о файле LFS
git lfs pointer --file=path/to/file.fbx

# Проверить целостность файлов LFS
git lfs fsck

# Очистить кэш LFS (если нужно освободить место)
git lfs prune
```

## ⚠️ Важные замечания

1. **Размер репозитория**: Git LFS хранит большие файлы отдельно, что уменьшает размер основного репозитория, но требует настройки LFS на сервере (GitHub, GitLab и т.д.)

2. **Квоты**: Многие хостинги Git (GitHub, GitLab) имеют ограничения на использование Git LFS. Проверьте квоты вашего хостинга.

3. **Клонирование**: При клонировании репозитория с Git LFS, файлы LFS загружаются автоматически, если Git LFS установлен.

4. **Без Git LFS**: Если кто-то клонирует репозиторий без установленного Git LFS, большие файлы не будут загружены (только указатели).

## 🆘 Решение проблем

### Проблема: Git LFS не установлен
**Решение**: Установите Git LFS согласно инструкциям выше.

### Проблема: Файлы не отслеживаются через LFS
**Решение**: 
1. Проверьте, что `.gitattributes` находится в корне проекта
2. Убедитесь, что выполнили `git lfs install`
3. Пересоздайте файлы или используйте `git lfs migrate`

### Проблема: Ошибки при push - "Remote does not support the Git LFS locking API"
**Решение**: 
Отключите проверку блокировок LFS (это нормально для GitHub):
```bash
git config lfs.https://github.com/Batr01/RPG.git/info/lfs.locksverify false
```

Или для всех репозиториев:
```bash
git config --global lfs.locksverify false
```

### Проблема: Ошибки credentials при push
**Решение**: 
1. Настройте аутентификацию GitHub:
   ```bash
   # Используйте Personal Access Token вместо пароля
   git config --global credential.helper osxkeychain
   ```
2. Или используйте SSH вместо HTTPS:
   ```bash
   git remote set-url origin git@github.com:Batr01/RPG.git
   ```

### Проблема: Ошибки в .gitattributes с путями, содержащими пробелы
**Решение**: 
Git attributes не поддерживает пути с пробелами напрямую. Вместо этого используются правила по расширениям файлов (`.fbx`, `.png`, `.unitypackage` и т.д.), которые автоматически применяются ко всем файлам, включая файлы в папках с пробелами в названиях.

## 📚 Дополнительные ресурсы

- Официальная документация Git LFS: https://git-lfs.github.com/
- Документация GitHub по Git LFS: https://docs.github.com/en/repositories/working-with-files/managing-large-files/about-git-large-file-storage
