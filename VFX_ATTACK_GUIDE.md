# ⚔️ Подробный гайд: VFX эффект атаки мечом

Этот гайд покажет, как создать и привязать визуальный эффект (VFX) к атаке мечом. Вы уже настроили атаку, теперь добавим эффекты для большей визуальной выразительности.

---

## 📋 Содержание

1. [Подготовка](#подготовка)
2. [Visual Effect Graph - Создание эффекта](#visual-effect-graph---создание-эффекта)
3. [Интеграция VFX Graph с системой атаки](#интеграция-vfx-graph-с-системой-атаки)
4. [Настройка эффекта на оружии](#настройка-эффекта-на-оружии)
5. [Дополнительные эффекты](#дополнительные-эффекты)
6. [Оптимизация](#оптимизация)
7. [Отладка](#отладка)

---

## 🎯 Подготовка

### Что вам понадобится:

- ✅ Unity проект с настроенной системой атаки (уже есть)
- ✅ Префаб игрока с компонентом `PlayerCombat`
- ✅ Оружие с компонентом `DamageDealer`
- ✅ Анимации атаки с Animation Events

### Где разместить эффект:

VFX эффект можно разместить:
- **На оружии** (рекомендуется) - эффект следует за мечом
- **На руке персонажа** - эффект в точке захвата
- **На отдельном пустом объекте** - для более сложных эффектов

---

## 🎨 Visual Effect Graph - Создание эффекта

Visual Effect Graph (VFX Graph) - это продвинутая система визуальных эффектов Unity, которая предоставляет мощные возможности для создания красивых эффектов атаки.

> **Примечание:** Если вы уже создали VFX Graph (`SwordAttackVFX.vfx`), переходите к разделу [Интеграция VFX Graph с системой атаки](#интеграция-vfx-graph-с-системой-атаки).

### Шаг 1: Установка пакета (ОБЯЗАТЕЛЬНО!)

> ⚠️ **ВАЖНО:** Без установленного пакета Visual Effect Graph эффект работать НЕ БУДЕТ!
> 
> Если вы видите ошибку в Console: "Visual Effect Graph не установлен!" - выполните эти шаги:

1. `Window` → `Package Manager`
2. Выберите `Unity Registry` (вкладка сверху)
3. Найдите `Visual Effect Graph` в списке
4. Нажмите `Install` (справа внизу)
5. Дождитесь завершения установки
6. **Перезапустите Unity Editor** (закройте и откройте заново)

> ✅ После установки ошибка "Visual Effect Graph не установлен" должна исчезнуть из Console.

### Шаг 2: Создание VFX Graph

1. `Right Click` в Project → `Create` → `Visual Effects` → `Visual Effect Graph`
2. Назовите: `SwordAttackVFX`
3. Дважды кликните для открытия редактора VFX Graph

### Шаг 3: Базовая настройка VFX Graph

1. **Создайте Spawn System:**
   - В окне VFX Graph найдите систему "Spawn System"
   - Добавьте событие `Single Burst` (или `Burst`)
   - Установите `Count`: 30 (количество частиц при активации)

2. **Создайте Initialize Particle:**
   - Добавьте узел `Initialize Particle` (обычно уже есть по умолчанию)
   - Настройте:
     - `Lifetime`: 0.2 (время жизни частиц)
     - `Position`: Используйте `Set Position (Shape: Line)` - для эффекта вдоль лезвия
     - `Velocity`: Направление вперед

3. **Настройка Set Position (Shape: Line) для эффекта меча:**
   - В VFX Graph выберите узел "Set Position Shape" (или создайте: `Right Click` → `Create Node` → `Position` → `Set Position (Shape)`)
   - В **Inspector** найдите поле **"Shape"** (выпадающий список)
   - Откройте список и выберите **"Line"** (вместо "Sphere")
   - После выбора "Line" появятся параметры:
     - **Line Start**: (0, 0, 0) - начало линии
     - **Line End**: (0, 0, 0.5) - конец линии (настройте под длину вашего меча)
     - **Spawn Mode**: `Random` или `Custom` - как частицы распределяются по линии
   
   > **Примечание:** "Line" идеально подходит для эффекта вдоль лезвия меча. "Line" создает частицы вдоль прямой линии, что отлично подходит для следа меча.

4. **Альтернатива - использовать Circle для эффекта вокруг точки:**
   - Если нужен эффект вокруг точки удара, выберите **"Circle"** в списке "Shape"
   - Настройте **Radius**: 0.3-0.5 (радиус круга)
   - Настройте **Arc**: 360 (полный круг) или меньше для дуги

5. **Подключите узлы:**
   - Подключите выход `Position` из `Set Position (Shape: Line)` к входу `Position` в `Initialize Particle`

6. **Добавьте визуализацию:**
   - Узел `Update Particle` (опционально, для дополнительных эффектов)
   - Узел `Output Particle Quad` - для отображения частиц
   - Настройте цвет и размер в `Output Particle Quad`

7. **Сохраните VFX Graph:**
   - Нажмите `Ctrl+S` (или `Cmd+S` на Mac) для сохранения
   - VFX Graph будет сохранен как `.vfx` файл

---

## 🔗 Интеграция VFX Graph с системой атаки

### Шаг 1: Проверка скрипта

Скрипт `SwordAttackVFX.cs` уже готов к работе с Visual Effect Graph. Он содержит:
- Поддержку компонента `Visual Effect`
- Методы `PlayAttackEffect()` и `StopAttackEffect()` для управления VFX Graph
- Установку цвета через параметры VFX Graph
- Поддержку Trail Renderer (опционально)

### Шаг 2: Настройка компонента на оружии

1. **Найдите объект оружия в иерархии:**
   - Откройте префаб `Player` в `Assets/Prefabs/Player/Player.prefab`
   - Найдите объект `SwordVFX` (или создайте новый пустой GameObject на месте меча)

2. **Добавьте компоненты:**
   - `Add Component` → `Visual Effect` (для VFX Graph)
   - `Add Component` → `Sword Attack VFX` (скрипт)

3. **Настройте Visual Effect компонент:**
   - В поле `Visual Effect Asset` перетащите ваш `SwordAttackVFX.vfx`
   - Убедитесь, что `Play On Awake` отключен (чтобы эффект не запускался автоматически)

4. **Настройте скрипт SwordAttackVFX:**
   - В поле `Visual Effect` перетащите компонент `Visual Effect` с этого же объекта
   - Поле `Visual Effect` опционально - скрипт автоматически найдет компонент на этом же объекте

5. **Настройте PlayerCombat:**
   - Откройте корневой объект `Player`
   - В компоненте `PlayerCombat` найдите секцию "VFX Settings"
   - В поле `Sword VFX` перетащите объект с компонентом `SwordAttackVFX`

### Шаг 3: Проверка Animation Events

Убедитесь, что Animation Events настроены правильно:

1. **Откройте анимацию атаки:**
   - Найдите анимацию атаки в `Assets/Animations/Player/`
   - Или в Animator Controller

2. **Проверьте Events:**
   - На временной шкале должны быть события:
     - `OnAttackStart` - в начале активной фазы удара
     - `OnAttackEnd` - в конце активной фазы удара

3. **Если Events отсутствуют:**
   - Выберите кадр, где начинается удар
   - `Add Event` → Назовите: `OnAttackStart`
   - Выберите функцию: `PlayerCombat.OnAttackStart`
   - Повторите для `OnAttackEnd`

---

## 🔍 Отладка

Если VFX эффект не работает, см. подробный чек-лист в файле `VFX_DEBUG_CHECKLIST.md`.

Основные моменты для проверки:
1. **PlayerCombat** → `Sword VFX` назначен?
2. **SwordAttackVFX** → `Visual Effect` назначен?
3. **Visual Effect** → `Visual Effect Asset` назначен?
4. **Animation Events** добавлены в анимацию?
5. В **Console** есть сообщения при атаке?

---

## ⚡ Дополнительная информация

### Параметры VFX Graph для управления из скрипта

Скрипт `SwordAttackVFX.cs` может управлять параметрами VFX Graph. Для этого добавьте параметры в ваш VFX Graph:

1. **Откройте VFX Graph:**
   - Дважды кликните на `SwordAttackVFX.vfx`

2. **Добавьте параметры:**
   - В окне VFX Graph найдите панель "Blackboard" (обычно слева)
   - Нажмите `+` для добавления параметра
   - Добавьте параметры:
     - **Color** (Vector4 или Vector3) - для изменения цвета эффекта
     - **Intensity** (Float) - для изменения интенсивности

3. **Используйте параметры в графе:**
   - Подключите параметр `Color` к узлу цвета в `Output Particle Quad`
   - Подключите параметр `Intensity` к количеству частиц в `Spawn System`

Теперь скрипт сможет управлять этими параметрами через методы `SetEffectColor()` и `SetIntensity()`.

---

## 📚 Полезные ссылки

- [Unity Visual Effect Graph Documentation](https://docs.unity3d.com/Packages/com.unity.visualeffectgraph@latest)
- [Trail Renderer Documentation](https://docs.unity3d.com/Manual/class-TrailRenderer.html)
- [Animation Events Documentation](https://docs.unity3d.com/Manual/animeditor-AnimationEvents.html)

---

## 🎯 Настройка компонента Visual Effect на оружии

1. **Найдите объект оружия в иерархии:**
   - Откройте префаб `Player` в `Assets/Prefabs/Player/Player.prefab`
   - Найдите объект `SwordVFX` (или создайте новый пустой GameObject на месте меча)

2. **Добавьте компоненты:**
   - `Add Component` → `Visual Effect` (для VFX Graph)
   - `Add Component` → `Sword Attack VFX` (скрипт)

3. **Настройте Visual Effect компонент:**
   - В поле `Visual Effect Asset` перетащите ваш `SwordAttackVFX.vfx`
   - Убедитесь, что `Play On Awake` отключен (чтобы эффект не запускался автоматически)

4. **Настройте скрипт SwordAttackVFX:**
   - В поле `Visual Effect` перетащите компонент `Visual Effect` с этого же объекта
   - Поле `Visual Effect` опционально - скрипт автоматически найдет компонент на этом же объекте

5. **Настройте PlayerCombat:**
   - Откройте корневой объект `Player`
   - В компоненте `PlayerCombat` найдите секцию "VFX Settings"
   - В поле `Sword VFX` перетащите объект с компонентом `SwordAttackVFX`

---

## 🔗 Интеграция VFX Graph с системой атаки

> ✅ **Важно:** Скрипт `SwordAttackVFX.cs` уже обновлен и работает только с Visual Effect Graph!
> 
> Все упоминания Particle System удалены. Скрипт использует только компонент `Visual Effect` для работы с VFX Graph.

### Шаг 1: Проверка интеграции с PlayerCombat

> ✅ **Важно:** Интеграция с `PlayerCombat.cs` уже выполнена!
>
> Скрипт `PlayerCombat.cs` уже содержит:
> - Поле `[SerializeField] private SwordAttackVFX swordVFX;` в секции "VFX Settings"
> - Вызовы `swordVFX.PlayAttackEffect()` в `OnAttackStart()`
> - Вызовы `swordVFX.StopAttackEffect()` в `OnAttackEnd()`
> - Установку цвета эффекта в зависимости от комбо-индекса
>
> **Проверьте в Unity:**
> - Откройте префаб Player
> - В компоненте `PlayerCombat` должна быть секция "VFX Settings"
> - Поле "Sword VFX" должно быть пустым (назначим позже)

### (Для справки) Что уже интегрировано:

Скрипт `PlayerCombat.cs` уже содержит следующую интеграцию:

```csharp
[Header("VFX Settings")]
[SerializeField] private SwordAttackVFX swordVFX;

// В методе OnAttackStart():
if (swordVFX != null)
{
    swordVFX.PlayAttackEffect();
}

// В методе OnAttackEnd():
if (swordVFX != null)
{
    swordVFX.StopAttackEffect();
}

// В методе ExecuteAttack():
if (swordVFX != null)
{
    swordVFX.SetComboColor(_currentComboIndex);
}
```

Все готово! Переходите к настройке компонентов в Unity.

---

## 🎯 Настройка эффекта на оружии

### Шаг 1: Добавление компонентов

1. **Откройте префаб Player:**
   - `Assets/Prefabs/Player/Player.prefab`

2. **Найдите объект оружия:**
   - Обычно это дочерний объект модели персонажа
   - Если оружия нет отдельно, создайте пустой GameObject на месте меча

3. **Добавьте компоненты:**
   - `Visual Effect` (для VFX Graph)
   - `Sword Attack VFX` (скрипт)
   - `Trail Renderer` (опционально, для дополнительного следа)

### Шаг 2: Настройка в Inspector

1. **В компоненте `Visual Effect`:**
   ```
   - Visual Effect Asset: Перетащите ваш SwordAttackVFX.vfx
   - Initial Event Name: Оставьте "OnPlay" (это нормально)
   - ⚠️ ВАЖНО: Скрипт автоматически остановит эффект при старте
   ```
   
   > **Примечание:** Если эффект все равно запускается при старте, это нормально - скрипт автоматически остановит его при инициализации. Эффект будет запускаться только при атаке через Animation Events.

2. **В компоненте `Sword Attack VFX`:**
   ```
   - Visual Effect: (опционально) Перетащите компонент Visual Effect с этого же объекта
     ⚠️ Если поле не видно или пустое - не страшно! Скрипт автоматически найдет компонент Visual Effect на этом же объекте.
   - Sword Trail: Перетащите Trail Renderer (если есть, опционально)
   - Play On Attack Start: ✓
   - Stop On Attack End: ✓
   - Trail Duration: 0.3
   ```
   
   > **Важно:** Скрипт автоматически найдет компонент `Visual Effect` на том же объекте, если поле не назначено вручную. Но для надежности лучше назначить его явно.

3. **В компоненте `PlayerCombat`:**
   ```
   - Sword VFX: Перетащите объект с компонентом SwordAttackVFX
   ```

### Шаг 3: Настройка Animation Events

Убедитесь, что Animation Events настроены правильно:

1. **Откройте анимацию атаки:**
   - Найдите анимацию атаки в `Assets/Animations/Player/`
   - Или в Animator Controller

2. **Проверьте Events:**
   - На временной шкале должны быть события:
     - `OnAttackStart` - в начале активной фазы удара
     - `OnAttackEnd` - в конце активной фазы удара

3. **Если Events отсутствуют:**
   - Выберите кадр, где начинается удар
   - `Add Event` → Назовите: `OnAttackStart`
   - Выберите функцию: `PlayerCombat.OnAttackStart`
   - Повторите для `OnAttackEnd`

---

## ✨ Дополнительные эффекты

### Эффект попадания (Hit Effect)

Создайте эффект, который появляется при попадании по врагу:

1. **Создайте новый Particle System:**
   - Назовите: `HitEffect`
   - Настройте как короткую вспышку

2. **Обновите `DamageDealer.cs`:**

Добавьте в начало класса:
```csharp
[Header("VFX")]
[SerializeField] private GameObject hitEffectPrefab;
[SerializeField] private float hitEffectLifetime = 2f;
```

Добавьте в метод `OnTriggerEnter` после нанесения урона:
```csharp
// Создать эффект попадания
if (hitEffectPrefab != null)
{
    GameObject hitEffect = Instantiate(hitEffectPrefab, other.ClosestPoint(transform.position), Quaternion.identity);
    Destroy(hitEffect, hitEffectLifetime);
}
```

### Эффект заряда (для комбо-атак)

Для более мощных комбо-атак можно добавить эффект накопления энергии:

1. **Создайте Particle System с градиентом:**
   - Цвет меняется в зависимости от индекса комбо
   - Интенсивность увеличивается с каждым ударом

2. **Обновите `SwordAttackVFX.cs`:**

Добавьте метод для установки цвета в зависимости от комбо:
```csharp
public void SetComboColor(int comboIndex)
{
    Color[] comboColors = {
        Color.white,           // 1-й удар
        Color.yellow,          // 2-й удар
        Color.red              // 3-й удар
    };

    int colorIndex = Mathf.Clamp(comboIndex, 0, comboColors.Length - 1);
    SetEffectColor(comboColors[colorIndex]);
}
```

3. **Вызывайте в `PlayerCombat.ExecuteAttack()`:**
```csharp
if (swordVFX != null)
{
    swordVFX.SetComboColor(_currentComboIndex);
}
```

### Эффект ветра/воздуха

Для создания эффекта разрезания воздуха:

1. **Создайте Particle System с формой "Edge":**
   - Частицы летят перпендикулярно лезвию
   - Используйте текстуру с прозрачностью

2. **Настройте Velocity:**
   - Разные направления для создания эффекта "ветра"

---

## 🚀 Оптимизация

### Производительность

1. **Ограничьте количество частиц:**
   - Используйте `Max Particles` в Particle System
   - Не более 100-200 частиц на эффект

2. **Используйте Object Pooling:**
   - Для эффектов попадания создайте пул объектов
   - Переиспользуйте эффекты вместо создания новых

3. **Отключите ненужные модули:**
   - В Particle System отключите модули, которые не используете
   - Это снижает нагрузку на CPU

4. **Используйте LOD (Level of Detail):**
   - Для дальних объектов используйте упрощенные эффекты
   - Отключайте эффекты за пределами видимости

### Настройки качества

Создайте систему, которая адаптирует эффекты под настройки качества:

```csharp
public void SetQualityLevel(int qualityLevel)
{
    if (visualEffect != null)
    {
        // Управление через параметры VFX Graph
        switch (qualityLevel)
        {
            case 0: // Low
                if (visualEffect.HasFloat("ParticleCount"))
                    visualEffect.SetFloat("ParticleCount", 20f);
                break;
            case 1: // Medium
                if (visualEffect.HasFloat("ParticleCount"))
                    visualEffect.SetFloat("ParticleCount", 50f);
                break;
            case 2: // High
                if (visualEffect.HasFloat("ParticleCount"))
                    visualEffect.SetFloat("ParticleCount", 100f);
                break;
        }
    }
}
```

---

## ✅ Чек-лист настройки

После выполнения всех шагов проверьте:

- [ ] Particle System создан и настроен
- [ ] Скрипт `SwordAttackVFX.cs` создан и добавлен на оружие
- [ ] Компонент `SwordAttackVFX` назначен в `PlayerCombat`
- [ ] Animation Events настроены (`OnAttackStart`, `OnAttackEnd`)
- [ ] Эффект активируется при атаке
- [ ] Эффект останавливается после атаки
- [ ] Trail Renderer работает корректно (если используется)
- [ ] Материал для эффекта создан и применен
- [ ] Эффект виден в Game View

---

## 🐛 Решение проблем

### Эффект не появляется

1. **Проверьте, что Particle System не отключен:**
   - В Inspector убедитесь, что компонент активен
   - Проверьте, что `Play On Awake` отключен

2. **Проверьте Animation Events:**
   - Убедитесь, что события вызывают правильные методы
   - Проверьте, что объект с `PlayerCombat` назначен в Animator

3. **Проверьте ссылки:**
   - В `PlayerCombat` должна быть назначена ссылка на `SwordAttackVFX`

### Эффект появляется не в том месте

1. **Проверьте позицию объекта:**
   - Убедитесь, что объект с Particle System находится на оружии
   - Проверьте `Simulation Space` (должно быть `Local`)

2. **Проверьте Transform:**
   - Убедитесь, что позиция и поворот корректны
   - Используйте Gizmos для визуализации

### Эффект слишком слабый/сильный

1. **Настройте параметры Particle System:**
   - Увеличьте/уменьшите `Start Size`
   - Измените `Start Color` на более яркий
   - Увеличьте количество частиц в `Burst`

2. **Настройте материал:**
   - Используйте более яркий цвет
   - Добавьте эмиссию (Emission) в материал

---

## 📚 Дополнительные ресурсы

- [Unity Particle System Documentation](https://docs.unity3d.com/Manual/PartSys.html)
- [Trail Renderer Documentation](https://docs.unity3d.com/Manual/class-TrailRenderer.html)
- [Visual Effect Graph Documentation](https://docs.unity3d.com/Packages/com.unity.visualeffectgraph@latest)

---

## 🎉 Готово!

Теперь ваша атака мечом имеет визуальные эффекты! Экспериментируйте с настройками, чтобы создать уникальный стиль для вашей игры.

**Совет:** Сохраните разные варианты эффектов как префабы, чтобы быстро переключаться между ними и находить идеальный вариант.
