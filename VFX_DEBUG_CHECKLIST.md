# 🔍 Чек-лист отладки VFX эффекта атаки

Если VFX эффект не срабатывает при атаке, проверьте каждый пункт по порядку:

---

## ✅ Шаг 1: Проверка ссылок в PlayerCombat

1. **Откройте префаб Player:**
   - `Assets/Prefabs/Player/Player.prefab`

2. **Выберите корневой объект `Player`**

3. **В компоненте `PlayerCombat` найдите секцию "VFX Settings":**
   - Поле **"Sword VFX"** должно быть заполнено
   - Если пустое → перетащите объект с компонентом `SwordAttackVFX`

4. **Проверьте в Console:**
   - При атаке должно появиться сообщение: `"PlayerCombat: OnAttackStart вызван, VFX активирован"`
   - Если видите: `"PlayerCombat: swordVFX не назначен!"` → назначьте ссылку

---

## ✅ Шаг 2: Проверка компонента SwordAttackVFX

1. **Найдите объект с компонентом `SwordAttackVFX`:**
   - Обычно это `SwordVFX` или объект на оружии

2. **Проверьте компонент `Sword Attack VFX`:**
   - **Visual Effect** (для VFX Graph):
     - Должен быть назначен компонент `Visual Effect` с этого же объекта
     - Если пустое → перетащите компонент `Visual Effect`
   
   - **Attack Particles** (для Particle System):
     - Может быть пустым, если используете только VFX Graph

3. **Проверьте в Console:**
   - При атаке должно появиться: `"SwordAttackVFX: Visual Effect Graph запущен"`
   - Если видите: `"SwordAttackVFX: visualEffect не назначен!"` → назначьте ссылку

---

## ✅ Шаг 3: Проверка компонента Visual Effect

1. **На том же объекте проверьте компонент `Visual Effect`:**

2. **Проверьте настройки:**
   - **Visual Effect Asset**: должен быть назначен ваш `SwordAttackVFX.vfx`
   - **Play On Awake**: должен быть **ОТКЛЮЧЕН** (unchecked)
   - **Initial Event Name**: обычно `OnPlay`

3. **Если Visual Effect Asset не назначен:**
   - Найдите ваш VFX Graph: `Assets/VFX/test/SwordAttackVFX.vfx`
   - Перетащите его в поле `Visual Effect Asset`

---

## ✅ Шаг 4: Проверка Animation Events

1. **Откройте окно Animation:**
   - `Window` → `Animation` → `Animation`

2. **Выберите префаб Player в иерархии**

3. **Выберите анимацию атаки** (например, `Attack1`)

4. **Проверьте временную шкалу:**
   - Должны быть события (маленькие белые треугольники):
     - `OnAttackStart` - в начале активной фазы удара
     - `OnAttackEnd` - в конце активной фазы удара

5. **Если событий нет, добавьте их:**
   - Найдите момент начала удара (обычно 30-40% анимации)
   - Нажмите `Add Event` (или правый клик на timeline → `Add Animation Event`)
   - В Inspector события:
     - **Function**: выберите `OnAttackStart`
     - **Object**: должен быть выбран объект с `PlayerCombat`
   
   - Повторите для `OnAttackEnd` (обычно 60-70% анимации)

6. **Проверьте, что события вызывают правильную функцию:**
   - В Inspector события должно быть:
     - **Function**: `OnAttackStart` или `OnAttackEnd`
     - **Object**: объект с компонентом `PlayerCombat` (обычно корневой `Player`)

---

## ✅ Шаг 5: Проверка VFX Graph

1. **Откройте ваш VFX Graph:**
   - Дважды кликните на `SwordAttackVFX.vfx`

2. **Проверьте Spawn System:**
   - Должен быть настроен `Single Burst` или `Burst`
   - `Count` должен быть больше 0 (например, 30)

3. **Проверьте Initialize Particle:**
   - Должен быть узел `Initialize Particle`
   - `Lifetime` должен быть больше 0

4. **Проверьте Output:**
   - Должен быть узел `Output Particle Quad` или другой output
   - Без output частицы не будут отображаться

5. **Проверьте, что VFX Graph сохранен:**
   - Нажмите `Ctrl+S` (или `Cmd+S` на Mac)
   - Убедитесь, что нет ошибок компиляции в окне VFX Graph

---

## ✅ Шаг 6: Проверка в Play Mode

1. **Запустите игру (Play Mode)**

2. **Нажмите кнопку атаки**

3. **Проверьте Console:**
   - Должны появиться сообщения:
     - `"PlayerCombat: OnAttackStart вызван, VFX активирован"`
     - `"SwordAttackVFX: Visual Effect Graph запущен"`

4. **Если сообщений нет:**
   - Animation Events не вызываются → проверьте Шаг 4
   - Если есть первое сообщение, но нет второго → проверьте Шаг 2

5. **Если сообщения есть, но эффект не виден:**
   - Проверьте, что объект с Visual Effect не скрыт
   - Проверьте, что камера видит объект
   - Проверьте настройки VFX Graph (Шаг 5)

---

## ✅ Шаг 7: Дополнительные проверки

### Проверка позиции объекта

1. **Убедитесь, что объект с Visual Effect находится на оружии:**
   - Объект должен быть дочерним объектом оружия или руки
   - Позиция должна быть в месте, где должен быть эффект

2. **Проверьте Transform:**
   - Position не должен быть (0, 0, 0) если оружие не в начале координат
   - Rotation должен соответствовать ориентации оружия

### Проверка слоев и видимости

1. **Проверьте Layer объекта:**
   - Объект не должен быть на слое, который не виден камере

2. **Проверьте, что объект активен:**
   - В иерархии объект должен быть активен (галочка включена)

### Проверка версии Unity и пакетов

1. **Проверьте, что Visual Effect Graph установлен:**
   - `Window` → `Package Manager`
   - Найдите `Visual Effect Graph`
   - Должен быть установлен

2. **Проверьте версию Unity:**
   - VFX Graph требует Unity 2019.3+ с URP или HDRP

---

## 🐛 Частые проблемы и решения

### Проблема: "swordVFX не назначен"
**Решение:** В `PlayerCombat` → `VFX Settings` → `Sword VFX` перетащите объект с компонентом `SwordAttackVFX`

### Проблема: "visualEffect не назначен"
**Решение:** В компоненте `SwordAttackVFX` → `Visual Effect` перетащите компонент `Visual Effect` с того же объекта

### Проблема: Animation Events не вызываются
**Решение:** 
- Проверьте, что события добавлены в анимацию
- Проверьте, что функция называется точно `OnAttackStart` и `OnAttackEnd`
- Проверьте, что Object в событии указывает на объект с `PlayerCombat`

### Проблема: VFX Graph не воспроизводится
**Решение:**
- Проверьте, что `Visual Effect Asset` назначен в компоненте `Visual Effect`
- Проверьте, что `Play On Awake` отключен
- Проверьте, что VFX Graph сохранен и скомпилирован без ошибок

### Проблема: Эффект виден, но не в том месте
**Решение:**
- Проверьте позицию объекта с Visual Effect
- Убедитесь, что объект является дочерним объектом оружия
- Проверьте настройки `Set Position` в VFX Graph

---

## 📝 Быстрая проверка (30 секунд)

1. ✅ `PlayerCombat` → `Sword VFX` назначен?
2. ✅ `SwordAttackVFX` → `Visual Effect` назначен?
3. ✅ `Visual Effect` → `Visual Effect Asset` назначен?
4. ✅ Animation Events добавлены в анимацию?
5. ✅ В Console есть сообщения при атаке?

Если все ✅, но эффект не работает → проверьте VFX Graph настройки (Шаг 5).

---

## 💡 Совет

Включите подробные логи в Console:
- `Window` → `Console`
- В настройках Console включите `Collapse` и `Clear on Play` для удобства

Теперь при каждой атаке вы будете видеть, на каком этапе происходит проблема.
