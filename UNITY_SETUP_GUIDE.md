# 🎮 Подробная инструкция по настройке в Unity

Все скрипты созданы! Теперь нужно настроить проект в Unity Editor. Следуйте этим инструкциям пошагово.

---

## ✅ Этап 1: Настройка тегов и слоев

### 1.1 Настройка Tags (Теги)

1. Откройте Unity Editor
2. `Edit` → `Project Settings` → `Tags and Layers`
3. В секции **Tags** добавьте:
   - Нажмите `+` и добавьте тег: `Enemy`
   - Тег `Player` уже должен быть (проверьте)

### 1.2 Настройка Layers (Слои)

В секции **Layers** настройте:
- **Layer 8**: `Player` (уже есть)
- **Layer 9**: `Enemy` (нажмите на пустой слой → введите "Enemy")
- **Layer 12**: `PlayerWeapon` (нажмите на пустой слой → введите "PlayerWeapon")
- **Layer 13**: `EnemyWeapon` (нажмите на пустой слой → введите "EnemyWeapon")

### 1.3 Настройка Layer Collision Matrix

1. `Edit` → `Project Settings` → `Physics`
2. Прокрутите вниз до **Layer Collision Matrix**
3. Настройте взаимодействия:
   - **PlayerWeapon (Layer 12)**:
     - ✓ Включите взаимодействие с `Enemy` (Layer 9)
     - ✗ Отключите со всеми остальными слоями
   - **EnemyWeapon (Layer 13)**:
     - ✓ Включите взаимодействие с `Player` (Layer 8)
     - ✗ Отключите со всеми остальными слоями

> **Важно**: Это предотвращает урон по "воздуху" и обеспечивает, что оружие игрока бьёт только врагов, а оружие врагов - только игрока.

---

## ✅ Этап 2: Настройка префаба Player

### 2.1 Добавить HealthSystem

1. Найдите префаб `Player` в `Assets/Prefabs/Player/Player.prefab`
2. Откройте его (двойной клик)
3. Выберите корневой объект `Player`
4. `Add Component` → Найдите `Health System`
5. Настройте параметры:
   - **Max Health**: `100`

### 2.2 Создать коллайдер оружия игрока

1. В иерархии префаба Player найдите оружие (меч/оружие в руках)
   - Путь примерно: `Player` → `Armature` → `Spine` → `RightHand` → `Weapon`
2. Правый клик на объекте оружия → `Create Empty`
3. Назовите его: `WeaponCollider`
4. На `WeaponCollider` добавьте компоненты:
   
   **Capsule Collider:**
   - `Add Component` → `Capsule Collider`
   - ✓ **Is Trigger**: включите
   - **Direction**: выберите направление вдоль меча (обычно Y или Z)
   - **Radius**: 0.1-0.15
   - **Height**: длина меча (примерно 1.0-1.5)
   - **Center**: отрегулируйте так, чтобы капсула покрывала лезвие меча

   **DamageDealer:**
   - `Add Component` → `Damage Dealer`
   - **Show Debug Info**: можете включить для отладки

5. Установите Layer для `WeaponCollider`:
   - В Inspector вверху выберите `Layer` → `PlayerWeapon`

6. **Важно**: По умолчанию коллайдер должен быть **выключен**:
   - В Inspector рядом с `Capsule Collider` **снимите галочку** (деактивируйте компонент)

### 2.3 Настроить PlayerCombat

1. Выберите корневой объект `Player`
2. Найдите компонент `Player Combat`
3. Настройте новые параметры:

   **Damage Settings:**
   - **Base Damage**: `10`
   - **Combo Damage Multipliers**: 
     - Size: `3`
     - Element 0: `1`
     - Element 1: `1.5`
     - Element 2: `2`
   - **Enemy Layer**: выберите только `Enemy` (откройте dropdown и выберите `Enemy`)
   - **Weapon Damage Dealer**: перетащите объект `WeaponCollider` сюда

### 2.4 Добавить Animation Events на атаки игрока

1. Откройте окно Animation: `Window` → `Animation` → `Animation`
2. Выберите префаб Player в иерархии
3. В окне Animation выберите анимацию `Attack1`:

   **Найти момент удара** (обычно 30-40% анимации):
   - Прокрутите timeline, найдите кадр, где меч начинает движение удара
   - Нажмите кнопку `Add Event` (или правый клик на timeline → `Add Animation Event`)
   - В Inspector события выберите функцию: `OnAttackStart()`

   **Найти конец удара** (обычно 60-70% анимации):
   - Найдите кадр, где удар завершен
   - Добавьте событие: `OnAttackEnd()`

4. Повторите для `Attack2` и `Attack3`:
   - Для каждой анимации добавьте `OnAttackStart()` и `OnAttackEnd()`
   - Точное время зависит от анимации

> **Совет**: Для точной настройки можно использовать Gizmos - включите `Show Debug Info` в DamageDealer и смотрите, когда появляется красный wireframe коллайдера.

### 2.5 Сохранить префаб

- `File` → `Save` или `Ctrl+S`

---

## ✅ Этап 3: Создание префаба Enemy

### 3.1 Создать базовую структуру

1. В иерархии сцены: `Right Click` → `Create Empty`
2. Назовите: `Enemy`
3. Установите:
   - **Tag**: `Enemy`
   - **Layer**: `Enemy`
   - **Position**: (0, 0, 0)

### 3.2 Добавить модель врага

**Вариант A: Использовать готовый префаб Kevin Iglesias**

1. Найдите в Project:
   `Assets/Kevin Iglesias/Human Animations/Unity Demo Scenes/Human Melee Animations/Prefabs/Characters/`
2. Перетащите `HumanM_Dummy_Red - Sword and Shield.prefab` как **дочерний объект** Enemy
3. Переименуйте в `Model`

**Вариант B: Создать с нуля**

1. Найдите модель `HumanM_Model.fbx` в `Assets/Kevin Iglesias/Human Animations/Models/`
2. Перетащите как дочерний объект Enemy
3. Назначьте материалы и оружие вручную

### 3.3 Добавить компоненты на Enemy (корневой объект)

Выберите корневой объект `Enemy` и добавьте компоненты:

**1. Character Controller:**
```
Add Component → Character Controller
- Center: (0, 1, 0)
- Radius: 0.4
- Height: 1.8
- Slope Limit: 45
- Step Offset: 0.3
```

**2. Health System:**
```
Add Component → Health System
- Max Health: 50
```

**3. Enemy Controller:**
```
Add Component → Enemy Controller
- Show Debug Info: можете включить для отладки
```

**4. Enemy Movement:**
```
Add Component → Enemy Movement
- Move Speed: 3
- Rotation Speed: 5
- Stopping Distance: 1.5
- Gravity: -9.81
```

**5. Enemy AI:**
```
Add Component → Enemy AI
- Detection Radius: 10
- Attack Radius: 2
- Player Layer: выберите только "Player"
- Chase Update Interval: 0.2
- Lose Target Distance: 15
- Show Debug Info: можете включить
```

**6. Enemy Combat:**
```
Add Component → Enemy Combat
- Attack Cooldown: 2
- Attack Damage: 15
- Player Layer: выберите только "Player"
- Weapon Damage Dealer: (назначим позже)
```

**7. Enemy Animation Controller:**
```
Add Component → Enemy Animation Controller
- Speed Damp Time: 0.1
```

### 3.4 Найти Animator в модели

1. Разверните иерархию Enemy → Model (дочерний объект)
2. На объекте модели должен быть компонент `Animator`
3. Если его нет - добавьте: `Add Component` → `Animator`

### 3.5 Создать Animator Controller для врага

1. В Project создайте папку: `Assets/Animations/Enemy/`
2. Правый клик в папке → `Create` → `Animator Controller`
3. Назовите: `EnemyAnimator`
4. Двойной клик чтобы открыть Animator Window
5. Назначьте контроллер:
   - Выберите Model врага в иерархии
   - В компоненте Animator → **Controller**: перетащите `EnemyAnimator`

### 3.6 Настроить Animator Controller

**Создать параметры:**

В окне Animator перейдите на вкладку `Parameters`:
1. `+` → `Float` → назовите `Speed`
2. `+` → `Trigger` → назовите `Attack`
3. `+` → `Bool` → назовите `IsDead`

**Создать состояния (States):**

1. **Idle State** (уже есть как Entry):
   - Правый клик → `Create State` → `Empty`
   - Назовите: `Idle`
   - В Inspector → **Motion**: найдите idle анимацию из Kevin Iglesias
     - Путь: `Assets/Kevin Iglesias/.../Animations/` найдите idle анимацию

2. **Run/Walk State**:
   - Создайте новый State: `Run`
   - **Motion**: назначьте run/walk анимацию

3. **Attack State**:
   - Создайте: `Attack`
   - **Motion**: назначьте attack анимацию (Sword and Shield attack)

4. **Death State**:
   - Создайте: `Death`
   - **Motion**: назначьте death анимацию

**Создать переходы (Transitions):**

1. **Idle → Run**:
   - Правый клик на Idle → `Make Transition` → клик на Run
   - Выберите переход в списке
   - В Inspector:
     - **Conditions**: `Speed` `Greater` `0.1`
     - ✗ **Has Exit Time**: выключите

2. **Run → Idle**:
   - Run → Idle
   - **Conditions**: `Speed` `Less` `0.1`
   - ✗ **Has Exit Time**: выключите

3. **Run → Attack**:
   - Run → Attack
   - **Conditions**: `Attack` (trigger)
   - ✗ **Has Exit Time**: выключите

4. **Idle → Attack**:
   - Idle → Attack
   - **Conditions**: `Attack`

5. **Attack → Idle**:
   - Attack → Idle
   - ✓ **Has Exit Time**: включите
   - **Exit Time**: 0.9

6. **Any State → Death**:
   - Правый клик на `Any State` → `Make Transition` → Death
   - **Conditions**: `IsDead` `true`
   - ✗ **Has Exit Time**: выключите

### 3.7 Добавить Animation Events на атаку врага

1. В окне Animation выберите модель врага
2. Выберите анимацию Attack
3. Найдите момент начала удара (примерно середина анимации):
   - Добавьте событие: `OnAttackStart()`
4. Найдите конец удара:
   - Добавьте событие: `OnAttackEnd()`

### 3.8 Создать коллайдер оружия врага

1. В иерархии Enemy найдите оружие в руках модели
   - Примерно: `Enemy` → `Model` → `Armature` → `RightHand` → `Weapon`
2. На объекте оружия (или создайте дочерний `WeaponCollider`):
   
   **Capsule Collider:**
   - `Add Component` → `Capsule Collider`
   - ✓ **Is Trigger**: включите
   - Настройте размер под оружие

   **DamageDealer:**
   - `Add Component` → `Damage Dealer`

3. Установите **Layer**: `EnemyWeapon`
4. **Деактивируйте коллайдер** (снимите галочку)

### 3.9 Связать DamageDealer с EnemyCombat

1. Выберите корневой объект `Enemy`
2. Найдите компонент `Enemy Combat`
3. **Weapon Damage Dealer**: перетащите объект с DamageDealer (WeaponCollider)

### 3.10 Создать 3D Health Bar

**Создать Canvas:**

1. На корневом Enemy: `Right Click` → `UI` → `Canvas`
2. Назовите: `HealthBarCanvas`
3. Настройте Canvas:
   - **Render Mode**: `World Space`
   - **Width**: 100
   - **Height**: 20
   - **Position**: (0, 2.5, 0) - над головой врага
   - **Scale**: (0.01, 0.01, 0.01)

**Создать UI элементы:**

1. На HealthBarCanvas: `Right Click` → `UI` → `Image`
   - Назовите: `Background`
   - **Color**: Красный (255, 0, 0, 255)
   - **Anchors**: Stretch-Stretch (растянуть на весь Canvas)
   
2. На HealthBarCanvas: `Right Click` → `UI` → `Image`
   - Назовите: `Fill`
   - **Color**: Зелёный (0, 255, 0, 255)
   - **Anchors**: Left-Stretch
   - **Image Type**: `Filled`
   - **Fill Method**: `Horizontal`
   - **Fill Origin**: `Left`

**Добавить скрипт:**

1. На `HealthBarCanvas`:
   - `Add Component` → `Health Bar 3D`
   - **Fill Image**: перетащите объект `Fill`
   - **Billboard**: перетащите сам `HealthBarCanvas`
   - **Offset**: (0, 2.5, 0)
   - **Hide When Full**: можете включить
   - **Hide When Dead**: включите

### 3.11 Сохранить как префаб

1. Перетащите Enemy из иерархии в `Assets/Prefabs/Enemies/` (создайте папку Enemies)
2. Назовите: `Enemy_Melee`
3. Удалите Enemy из сцены или оставьте для тестирования

---

## ✅ Этап 4: Создание UI здоровья игрока

### 4.1 Найти или создать Canvas

1. Если в сцене нет Canvas: `Right Click` → `UI` → `Canvas`
2. Настройте Canvas:
   - **Render Mode**: `Screen Space - Overlay`
   - **Canvas Scaler**: `Scale With Screen Size`
   - **Reference Resolution**: 1920x1080

### 4.2 Создать панель здоровья

1. На Canvas: `Right Click` → `UI` → `Panel`
2. Назовите: `PlayerHealthPanel`
3. Настройте Rect Transform:
   - **Anchors**: Top-Left
   - **Position**: (120, -30)
   - **Size**: (200, 30)

### 4.3 Создать элементы UI

**Background:**
1. На PlayerHealthPanel: `Right Click` → `UI` → `Image`
2. Назовите: `Background`
3. **Color**: Тёмно-серый (50, 50, 50, 200)
4. **Anchors**: Stretch-Stretch

**HealthBar:**
1. На PlayerHealthPanel: `UI` → `Image`
2. Назовите: `HealthBar`
3. **Color**: Красный (220, 0, 0)
4. **Anchors**: Stretch-Stretch
5. **Image Type**: `Filled`
6. **Fill Method**: `Horizontal`

**HealthText:**
1. На PlayerHealthPanel: `UI` → `Text - TextMeshPro`
2. Назовите: `HealthText`
3. Настройте:
   - **Text**: "100 / 100"
   - **Font Size**: 18-24
   - **Alignment**: Center
   - **Color**: Белый
   - **Anchors**: Stretch-Stretch

### 4.4 Добавить скрипт

1. На `PlayerHealthPanel`:
   - `Add Component` → `Player Health UI`
   - **Health Bar Fill**: перетащите `HealthBar` Image
   - **Health Text**: перетащите `HealthText`
   - **Player Health System**: 
     - Либо перетащите Player из иерархии/префаба
     - Либо оставьте пустым (скрипт найдёт автоматически по тегу)
   - **Show Health Text**: ✓ включите
   - **Smooth Transition**: ✓ включите
   - **Transition Speed**: 5

---

## ✅ Этап 5: Тестирование

### 5.1 Чек-лист перед запуском

Проверьте, что всё настроено:

- [ ] Теги: `Player` и `Enemy` созданы
- [ ] Слои: Player(8), Enemy(9), PlayerWeapon(12), EnemyWeapon(13)
- [ ] Layer Collision Matrix настроена
- [ ] Player имеет HealthSystem
- [ ] PlayerCombat настроен с DamageDealer
- [ ] Animation Events добавлены на атаки игрока
- [ ] Префаб Enemy создан со всеми компонентами
- [ ] Animator врага настроен с параметрами и переходами
- [ ] Animation Events добавлены на атаку врага
- [ ] Коллайдеры оружия настроены (Trigger, правильные слои)
- [ ] 3D Health Bar над врагом
- [ ] UI здоровья игрока на экране

### 5.2 Тестовая сцена

1. Поместите Player в сцену (из префаба или уже есть)
2. Поместите Enemy в сцену на расстоянии ~5-10 юнитов от игрока
3. Нажмите **Play**

### 5.3 Сценарии тестирования

**Тест 1: Обнаружение**
- Приблизьтесь к врагу на расстояние < 10 юнитов
- ✅ Враг должен начать преследование (State: Chasing в debug)

**Тест 2: Движение**
- Враг движется к игроку плавно
- ✅ Анимация бега/ходьбы проигрывается
- ✅ Враг поворачивается лицом к игроку

**Тест 3: Атака врага**
- Подойдите к врагу на расстояние < 2 юнитов
- ✅ Враг переходит в состояние Attacking
- ✅ Анимация атаки проигрывается
- ✅ При попадании оружия - игрок получает урон
- ✅ UI здоровья игрока обновляется

**Тест 4: Атака игрока**
- Атакуйте врага (нажмите кнопку атаки)
- ✅ При попадании меча - враг получает урон
- ✅ 3D Health Bar над врагом обновляется
- ✅ В консоли появляется сообщение об уроне

**Тест 5: Комбо игрока**
- Выполните комбо из 3 ударов
- ✅ Урон должен увеличиваться: 10, 15, 20
- ✅ В консоли видны множители урона

**Тест 6: Смерть врага**
- Убейте врага (нанесите > 50 урона)
- ✅ Анимация смерти проигрывается
- ✅ Враг перестаёт двигаться и атаковать
- ✅ Health Bar исчезает
- ✅ Через 5 секунд объект удаляется

**Тест 7: Смерть игрока**
- Дайте врагу убить игрока
- ✅ Health Bar доходит до 0
- ✅ В консоли: "Игрок умер!"

### 5.4 Отладка проблем

**Враг не атакует игрока:**
- Проверьте Layer Collision Matrix
- Убедитесь, что EnemyWeapon взаимодействует с Player
- Проверьте, что Animation Events установлены
- Включите Show Debug Info в DamageDealer

**Игрок не наносит урон врагу:**
- Проверьте Layer игрока и коллайдера оружия
- Убедитесь, что PlayerWeapon взаимодействует с Enemy
- Проверьте Animation Events на атаках
- В PlayerCombat проверьте настройку Enemy Layer

**Враг не видит игрока:**
- Проверьте тег Player
- Увеличьте Detection Radius в EnemyAI
- Убедитесь, что Player Layer выбран в EnemyAI

**Health Bar не отображается:**
- Проверьте Canvas Render Mode (World Space для врага)
- Убедитесь, что HealthBar3D подписан на HealthSystem
- Проверьте Fill Image назначен

---

## 📊 Финальная проверка

После успешного тестирования у вас должна работать:

✅ **Система здоровья**: Игрок и враги имеют HP
✅ **Система урона**: Атаки наносят урон с правильными множителями
✅ **AI врага**: Обнаружение, преследование, атака
✅ **Анимации**: Все переходы работают корректно
✅ **UI**: Отображение здоровья игрока и врагов
✅ **Смерть**: Корректная обработка смерти

---

## 🎯 Следующие шаги (опционально)

После базовой настройки можно добавить:

1. **Больше врагов**: Создайте вариации префаба с разными параметрами
2. **Партиклы**: Эффекты при ударах и смерти
3. **Звуки**: Звуки ударов, крики врагов
4. **Улучшенный AI**: Патрулирование, групповые атаки
5. **Систему опыта**: Дроп опыта при смерти врага

---

## 🔧 Полезные горячие клавиши

- `Ctrl+D` - Дублировать объект
- `F` - Сфокусироваться на выбранном объекте
- `Ctrl+Shift+F` - Переместить объект к камере
- `Alt+Left Click` - Вращение камеры вокруг объекта
- `Ctrl+S` - Сохранить

---

## 📞 Проблемы?

Если что-то не работает:
1. Проверьте консоль на ошибки
2. Включите Debug Info в компонентах
3. Используйте Gizmos для визуализации радиусов и коллайдеров
4. Убедитесь, что все ссылки в Inspector назначены (нет "None")

Удачи! 🎮
